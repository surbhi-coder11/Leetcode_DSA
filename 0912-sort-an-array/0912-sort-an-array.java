import java.util.*;
class Solution {
    public int[] sortArray(int[] nums) {
        Arrays.sort(nums);
        // int n = nums.length;

        // for(int i=0;i<n-1;i++){

        //     for(int j=i+1;j<n;j++){
        //         if(nums[j]<nums[i]){
        //             int temp = nums[i];
        //             nums[i]= nums[j];
        //             nums[j] = temp;
        //         }
        //     }
        // }
        return nums;
    }
}